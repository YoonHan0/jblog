<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="post">

	
	
	<select id="getPostList" parameterType="long" resultType="postvo">
		<![CDATA[
			SELECT title, contents, reg_date AS regDate
			FROM post
			WHERE category_no = #{no };
		]]>
	
	</select>
	
	<select id="getRecentlyPost" parameterType="long" resultType="postvo">
		<![CDATA[
			SELECT title, contents, reg_date AS regDate
			FROM post
			WHERE category_no = #{no }
			ORDER BY reg_date ASC
			LIMIT 0,1;
		]]>
	
	</select>
	
	<select id="getPostByNo" parameterType="long" resultType="postvo">
		<![CDATA[
			SELECT title, contents, reg_date AS regDate
			FROM post
			WHERE no = #{no };
		]]>
	
	</select>
	
</mapper>


